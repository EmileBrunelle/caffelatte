- name: restart sshd
  ansible.builtin.systemd_service: { name: sshd, state: restarted }
- name: reload sysctl
  ansible.builtin.command: sysctl --system
- name: reload user systemd
  ansible.builtin.command: "{{ svc_systemctl }} daemon-reload"
