# cp secrets.example.yml secrets.yml && sops -e -i secrets.yml
# Le fichier chiffré SE COMMIT : c'est le but de SOPS.
rcon_password: "change-moi"
postgres_password: "change-moi"
# Contenu du floodgate.pem généré par Geyser/Floodgate (clé AES du hors-ligne
# Bedrock) — coller le fichier tel quel, pas juste un mot de passe.
floodgate_key: "change-moi"
