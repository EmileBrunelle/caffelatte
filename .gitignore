# Inventaire réel : contient IP/FQDN publics. Jamais dans le dépôt public.
ansible/inventory.yml
# Clé privée age : jamais commitée (la publique va dans .sops.yaml).
*.agekey
keys.txt
# Clé Floodgate (AES) : partagée via podman secret, jamais dans le dépôt.
floodgate.pem
*.retry
*.tfvars
.terraform/
*.tfstate*
HANDOFF.md
# Cache local d'ansible-lint / ansible-galaxy.
.ansible/
# Config du backend OpenTofu : contient la Customer Secret Key.
terraform/backend.hcl
capacite-arm.log
.veilleuse-wip/
